const min = 0;
const max = 100;
/*Виводить парні числа*/ 
for (let i = min; i <= 100; i+=1 ){
    if (i%2 === 0) {
        console.log('Парне число:', i);
    }
}
/*Виводить числа, які не дають остачу від ділення на 3*/
for (let i = min; i <= 100; i += 1){
    if (i%3 === 0) {
        console.log('Не дають остачу від ділення на 3:', i);
    }
}